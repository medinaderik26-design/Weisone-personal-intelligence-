[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "weisone-personal-intelligence"
version = "0.1.0"
description = "Person-owned continuity infrastructure for AI systems."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
